#include <stdio.h>

int main() {
    // Write C code here
   // printf("Try programiz.pro");
    char str[]="agsgsnyuniusnwy";
    int ln=strlen(str);
    int vCount=0;
    int vCount=0;
    for(int i=0;i<ln;i++){
        char ch=str[i];
        if(ch='a'||ch='i'||ch='e'||ch='o'||ch='u'){
            vCount++
        };
    }else if{
        cCount;
    }
    return 0;
}